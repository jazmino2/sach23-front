const nombreUsuario = document.getElementById("nombreUsuario");

// Cambiar el nombre de usuario
nombreUsuario.innerText = "Nuevo nombre de usuario";

const ingresarFecha = document.getElementById("ingresarFecha");

// Agregar evento al botón "Ingresar fecha"
ingresarFecha.addEventListener("click", () => {
  // Código para ingresar fecha
});

const verHorarios = document.getElementById("verHorarios");

// Agregar evento al botón "Ver horarios"
verHorarios.addEventListener("click", () => {
  // Código para ver horarios
});
